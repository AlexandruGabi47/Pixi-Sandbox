# Pixi-Sandbox

To run the app use:

    npm run dev
Or alternatively if you want to host it and run it on another device via LAN:

    npm run devHost

For a funny 'feature' add the following parameter to the browser link(also increase the amount of sps) and wait, some shapes may experience gravity more than others

    ?funnyFeature=1